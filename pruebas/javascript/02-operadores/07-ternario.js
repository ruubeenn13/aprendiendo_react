// Expresión ? 'si es true' : 'si es false'

let edad = 15;
let acceeso = edad > 17 ? 'Permitir acceso' : 'Denegar acceso';
console.log(acceeso); // Denegar acceso

edad = 25;
acceeso = edad > 17 ? 'Permitir acceso' : 'Denegar acceso';
console.log(acceeso); // Permitir acceso