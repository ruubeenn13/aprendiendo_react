// WHILE se usa cuando queremos ejecutar una porción de código mientras una condición sea verdadera

let i = 0;

// Imprime los números pares del 0 al 10
while (i <= 10) {
  if (i % 2 === 0) {
    console.log('Número par:', i);
  }
  i++;
}
